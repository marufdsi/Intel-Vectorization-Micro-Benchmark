#include<iostream>
#include<vector>
using namespace std;

int main(){
  typedef int64_t index;
  index A[100], b[100], C[100];
  for(index i=0; i<100; i++){
    C[i] = a[i] + B[i];
  }

  return 0;
}
